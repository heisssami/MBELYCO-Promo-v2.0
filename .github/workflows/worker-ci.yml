name: Worker CI

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  worker-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        ports:
          - 5432:5432
        env:
          POSTGRES_DB: mbelyco_promo_local
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
        options: >-
          --health-cmd="pg_isready -U user -d mbelyco_promo_local"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd="redis-cli ping || exit 1"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: postgresql://user:password@localhost:5432/mbelyco_promo_local
      REDIS_URL: redis://localhost:6379
      NODE_ENV: test
      LOG_LEVEL: info

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npx prisma generate
      - run: npx prisma db push
      - run: node prisma/seed.js
      - run: npm run build
      - name: Start Next.js
        run: |
          nohup npm start > .next_server.log 2>&1 &
          echo $! > next.pid
          for i in {1..120}; do
            if curl -sf http://localhost:3000/api/health >/dev/null; then
              echo "Server ready"
              break
            fi
            sleep 1
          done
      - name: Start worker
        run: |
          nohup npm run worker > worker.log 2>&1 &
          echo $! > worker.pid
          sleep 8
      - name: Run worker integration test
        run: node scripts/test-worker.mjs
      - name: Run MoMo webhook idempotency test
        run: node scripts/test-webhook-momo.mjs
      - name: Print logs on failure
        if: failure()
        run: |
          echo "---- worker.log ----"
          cat worker.log || true
          echo "---- .next_server.log ----"
          cat .next_server.log || true
